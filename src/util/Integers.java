package util;

/**
 * The Integers is used to keeping final integers
 */
public class Integers {
    /**
     * The port is a port that server is waiting with that
     */
    public static final int port = 1234;

    /**
     * The nThread is number of threads that will created in a thread pool
     */
    public static final int nThreads=20;
}